<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.myproject.mapper.UserMapper">

    <insert id="insert" parameterType="com.example.myproject.entity.User"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `user` (username, password, phone, email, create_time)
        VALUES (#{username}, #{password}, #{phone}, #{email}, NOW())
    </insert>

    <select id="findByUsername" resultType="com.example.myproject.entity.User">
        SELECT * FROM `user` WHERE username = #{username}
    </select>

    <select id="findById" resultType="com.example.myproject.entity.User">
        SELECT * FROM `user` WHERE id = #{id}
    </select>

    <select id="findByPhone" resultType="com.example.myproject.entity.User">
        SELECT * FROM `user` WHERE phone = #{phone}
    </select>

    <select id="findByEmail" resultType="com.example.myproject.entity.User">
        SELECT * FROM `user` WHERE email = #{email}
    </select>

</mapper>
